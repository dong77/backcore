akka {
    nxt {
        redis {
            host = "localhost"
            port = 6379
        }

        nxtClient {
            host = "localhost"
            port = 7876
        }

        mongodb {
            host = "localhost"
            port = 27017
        }
    }
}
